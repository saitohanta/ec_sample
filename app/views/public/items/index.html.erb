<h3>商品一覧<small>（全<%= @items.count %>件）</h3>
<% @items.each do |item| %>
  <%= link_to item_path(item) do %>
    <%= image_tag item.image, size: "130x100" %>
    <%= item.name %>
    ¥<%= item.with_tax_price.to_s(:delimited) %>
  <% end %>
<% end %>
<%= paginate @items %>