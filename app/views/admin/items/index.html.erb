<h4>商品一覧</h4>
<%= link_to "+", new_admin_item_path %>
商品ID
商品名
税抜価格
ジャンル
ステータス
<% @items.each do |item| %>
  <%= item.id %>
  <%= link_to item.name, admin_item_path(item) %>
  <%= item.price %>
  <%= item.genre.name %>
  <%= item.item_status %>
<% end %>